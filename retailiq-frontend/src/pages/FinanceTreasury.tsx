import { createStaticPage } from '@/pages/createStaticPage';

export default createStaticPage(
  'Treasury',
  'Owner-only treasury controls.',
  'This treasury route keeps the shell complete and gives the finance area a stable destination for future treasury workflows.',
);
