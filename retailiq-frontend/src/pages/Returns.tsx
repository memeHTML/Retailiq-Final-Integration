import { createStaticPage } from './createStaticPage';

export default createStaticPage(
  'Returns',
  'Customer returns and refund operations.',
  'This placeholder keeps the returns flow ready for a dedicated implementation.',
);

