import { createStaticPage } from './createStaticPage';

export default createStaticPage(
  'Reports',
  'Operational and financial reports.',
  'This placeholder keeps the reports route ready for branch-level implementation.',
);

