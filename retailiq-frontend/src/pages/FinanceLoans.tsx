import { createStaticPage } from '@/pages/createStaticPage';

export default createStaticPage(
  'Loans',
  'Owner-only loan applications.',
  'This loans route is present so the sidebar and routing tree can reference a real page without crashing.',
);
