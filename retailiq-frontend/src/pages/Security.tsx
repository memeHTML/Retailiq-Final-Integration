import { createStaticPage } from '@/pages/createStaticPage';

export default createStaticPage(
  'Security / MFA',
  'Account security and authenticator setup.',
  'This protected shell page gives users a clear place to manage authentication hardening and MFA-related settings.',
);
